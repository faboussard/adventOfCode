// sort both lists
// pair up the smallest numbers in the left list with the smallest in the right list
// = pair up the numbers
// calculate b - a
// sum the difference

#include <string>
#include <iostream>
#include <fstream>

int main()
{
    fopen("input.txt", "r");
    if (!is_open("input.txt"))
    std::getline ()

}